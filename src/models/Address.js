export default class Address {
    constructor(id, street, number, neighborhood, zipCode, complement, city, state) {

        this.id = id;
        this.street = street;
        this.number = number;
        this.neighborhood = neighborhood;
        this.zipCode = zipCode;
        this.complement = complement;
        this.city = city;
        this.state = state;

    }
}